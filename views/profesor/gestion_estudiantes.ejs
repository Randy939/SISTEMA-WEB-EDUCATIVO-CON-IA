<!DOCTYPE html>
<html class="light" lang="es">
  <head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>Gestión de Estudiantes</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link href="https://fonts.googleapis.com" rel="preconnect" />
    <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined"
      rel="stylesheet"
    />
    <script>
      tailwind.config = {
        darkMode: "class",
        theme: {
          extend: {
            colors: {
              primary: "#4396ea",
              "background-light": "#f6f7f8",

              "background-dark": "#111921",
            },
            fontFamily: {
              display: ["Inter", "sans-serif"],
            },
            borderRadius: {
              DEFAULT: "0.25rem",
              lg: "0.5rem",
              xl: "0.75rem",
              full: "9999px",
            },
          },
        },
      };
    </script>
    <link rel="stylesheet" href="/css/main.css" />
  </head>

  <body class="font-display bg-background-light dark:bg-background-dark">
    <div class="relative flex min-h-screen w-full">
      <%- include('../partials/siderbar_profesor', { active: active, user: user
      }) %>

      <main class="flex-1 p-8">
        <div class="max-w-6xl mx-auto">
          <header class="mb-8">
            <p
              class="text-slate-900 dark:text-white text-4xl font-black leading-tight tracking-[-0.033em]"
            >
              Gestión de Estudiantes
            </p>
            <p
              class="text-slate-500 dark:text-slate-400 text-base font-normal leading-normal mt-2"
            >
              Crea nuevas cuentas y administra a tus estudiantes.
            </p>
          </header>

          <div class="grid grid-cols-1 lg:grid-cols-3 gap-8">
            <div class="lg:col-span-1">
              <div
                class="bg-white dark:bg-slate-900 rounded-xl shadow-sm border border-slate-200 dark:border-slate-700 p-6"
              >
                <h3
                  class="text-lg font-bold text-slate-900 dark:text-white mb-4"
                >
                  Añadir Nuevo Estudiante
                </h3>
                <form
                  action="/profesor/estudiantes/crear"
                  method="POST"
                  class="space-y-4"
                >
                  <div>
                    <label
                      class="text-sm font-medium text-slate-800 dark:text-slate-300"
                      for="nombre"
                      >Nombre Completo</label
                    >
                    <input
                      class="mt-1 block w-full rounded-lg border-slate-300 bg-slate-50 dark:bg-slate-700..."
                      id="nombre"
                      name="nombre"
                      type="text"
                      required
                    />
                  </div>
                  <div>
                    <label
                      class="text-sm font-medium text-slate-800 dark:text-slate-300"
                      for="email"
                      >Correo Electrónico</label
                    >
                    <input
                      class="mt-1 block w-full rounded-lg border-slate-300 bg-slate-50 dark:bg-slate-700..."
                      id="email"
                      name="email"
                      type="email"
                      required
                    />
                  </div>
                  <div>
                    <label
                      class="text-sm font-medium text-slate-800 dark:text-slate-300"
                      for="password"
                      >Contraseña Inicial</label
                    >
                    <input
                      class="mt-1 block w-full rounded-lg border-slate-300 bg-slate-50 dark:bg-slate-700..."
                      id="password"
                      name="password"
                      type="text"
                      required
                    />
                  </div>
                  <button
                    class="flex w-full min-w-[84px] cursor-pointer items-center justify-center rounded-lg h-10 px-6 bg-primary text-white text-sm font-medium"
                    type="submit"
                  >
                    <span class="truncate">Crear Cuenta</span>
                  </button>
                </form>
              </div>
            </div>

            <div class="lg:col-span-2">
              <div
                class="bg-white dark:bg-slate-900 rounded-xl shadow-sm border border-slate-200 dark:border-slate-700"
              >
                <h3
                  class="text-lg font-bold text-slate-900 dark:text-white p-6 border-b dark:border-slate-700"
                >
                  Lista de Estudiantes (<%= estudiantes.length %>)
                </h3>

                <div class="overflow-x-auto">
                  <table class="w-full text-left">
                    <thead
                      class="text-xs text-slate-500 dark:text-slate-400 uppercase bg-slate-50 dark:bg-slate-800"
                    >
                      <tr>
                        <th scope="col" class="px-6 py-3">Nombre</th>
                        <th scope="col" class="px-6 py-3">Email</th>
                        <th scope="col" class="px-6 py-3">Acciones</th>
                      </tr>
                    </thead>
                    <tbody>
                      <% if (estudiantes.length === 0) { %>
                      <tr class="bg-white dark:bg-slate-900">
                        <td
                          colspan="3"
                          class="px-6 py-4 text-center text-slate-500"
                        >
                          No hay estudiantes registrados.
                        </td>
                      </tr>
                      <% } %>

                      <% estudiantes.forEach(estudiante => { %>
                      <tr
                        class="bg-white dark:bg-slate-900 border-b dark:border-slate-700 hover:bg-slate-50 dark:hover:bg-slate-800"
                      >
                        <td
                          class="px-6 py-4 font-medium text-slate-900 dark:text-white"
                        >
                          <%= estudiante.nombre %>
                        </td>
                        <td
                          class="px-6 py-4 text-slate-600 dark:text-slate-300"
                        >
                          <%= estudiante.email %>
                        </td>

                        <td class="px-6 py-4 flex items-center gap-4">
                          <a
                            href="/profesor/estudiantes/editar/<%= estudiante._id %>"
                            class="font-medium text-primary hover:underline text-sm"
                            >Editar</a
                          >

                          <form
                            action="/profesor/estudiantes/reset-password"
                            method="POST"
                            class="flex items-center gap-2"
                          >
                            <input
                              type="hidden"
                              name="estudianteId"
                              value="<%= estudiante._id %>"
                            />
                            <input
                              type="text"
                              name="nuevaPassword"
                              placeholder="Nueva pass"
                              class="form-input rounded-lg text-xs p-2 h-8 w-28 bg-slate-50 dark:bg-slate-700 border-slate-300 dark:border-slate-600"
                              required
                            />
                            <button
                              type="submit"
                              class="font-medium text-amber-600 hover:underline text-xs"
                            >
                              Resetear
                            </button>
                          </form>

                          <form
                            action="/profesor/estudiantes/eliminar"
                            method="POST"
                            onsubmit="return confirm('¿Estás seguro de que quieres eliminar a este estudiante? Esta acción es permanente.');"
                          >
                            <input
                              type="hidden"
                              name="estudianteId"
                              value="<%= estudiante._id %>"
                            />
                            <button
                              type="submit"
                              class="font-medium text-red-600 hover:underline text-sm"
                            >
                              Eliminar
                            </button>
                          </form>
                        </td>
                      </tr>
                      <% }) %>
                    </tbody>
                  </table>
                </div>
              </div>
            </div>
          </div>
        </div>
      </main>
    </div>
  </body>
</html>
