<!DOCTYPE html>
<html class="light" lang="es">
  <head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>Editar Estudiante</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link href="https://fonts.googleapis.com" rel="preconnect" />
    <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined"
      rel="stylesheet"
    />
    <script>
      tailwind.config = {
        darkMode: "class",
        theme: {
          extend: {
            colors: {
              primary: "#4396ea",
              "background-light": "#f6f7f8",

              "background-dark": "#111921",
            },
            fontFamily: {
              display: ["Inter", "sans-serif"],
            },
            borderRadius: {
              DEFAULT: "0.25rem",
              lg: "0.5rem",
              xl: "0.75rem",
              full: "9999px",
            },
          },
        },
      };
    </script>
    <style>
      .material-symbols-outlined {
        font-variation-settings:
          "FILL" 0,
          "wght" 400,
          "GRAD" 0,
          "opsz" 24;
      }
    </style>
  </head>

  <body class="font-display bg-background-light dark:bg-background-dark">
    <div class="relative flex min-h-screen w-full">
      <%- include('../partials/siderbar_profesor', { active: active, user: user
      }) %>

      <main class="flex-1 p-8">
        <div class="max-w-2xl mx-auto">
          <header class="mb-8">
            <a
              href="/profesor/estudiantes"
              class="flex items-center gap-1 text-sm text-primary hover:underline mb-2"
            >
              <span class="material-symbols-outlined">arrow_back</span>
              Volver a la lista
            </a>
            <p
              class="text-slate-900 dark:text-white text-4xl font-black leading-tight tracking-[-0.033em]"
            >
              Editar Estudiante
            </p>
          </header>

          <div
            class="bg-white dark:bg-slate-900 rounded-xl shadow-sm border border-slate-200 dark:border-slate-700 p-6"
          >
            <form
              action="/profesor/estudiantes/editar/<%= estudiante._id %>"
              method="POST"
              class="space-y-4"
            >
              <div>
                <label
                  class="text-sm font-medium text-slate-800 dark:text-slate-300"
                  for="nombre"
                  >Nombre Completo</label
                >
                <input
                  class="mt-1 block w-full rounded-lg border-slate-300 bg-slate-50 dark:bg-slate-700..."
                  id="nombre"
                  name="nombre"
                  type="text"
                  value="<%= estudiante.nombre %>"
                  required
                />
              </div>
              <div>
                <label
                  class="text-sm font-medium text-slate-800 dark:text-slate-300"
                  for="email"
                  >Correo Electrónico</label
                >
                <input
                  class="mt-1 block w-full rounded-lg border-slate-300 bg-slate-50 dark:bg-slate-700..."
                  id="email"
                  name="email"
                  type="email"
                  value="<%= estudiante.email %>"
                  required
                />
              </div>
              <div class="flex justify-end gap-3 pt-4">
                <a
                  href="/profesor/estudiantes"
                  class="flex min-w-[84px] cursor-pointer items-center justify-center rounded-lg h-10 px-6 bg-slate-200 text-slate-700 dark:bg-slate-700 dark:text-slate-200 text-sm font-medium"
                >
                  Cancelar
                </a>
                <button
                  class="flex min-w-[84px] cursor-pointer items-center justify-center rounded-lg h-10 px-6 bg-primary text-white text-sm font-medium"
                  type="submit"
                >
                  <span class="truncate">Guardar Cambios</span>
                </button>
              </div>
            </form>
          </div>
        </div>
      </main>
    </div>
  </body>
</html>
