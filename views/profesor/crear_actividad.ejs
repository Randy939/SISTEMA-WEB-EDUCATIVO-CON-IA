<!DOCTYPE html>
<html class="light" lang="es">
  <head>
    <meta charset="utf-8" />
    <meta content="width=device-width, initial-scale=1.0" name="viewport" />
    <title>Crear Nueva Actividad</title>
    <script src="https://cdn.tailwindcss.com?plugins=forms,container-queries"></script>
    <link href="https://fonts.googleapis.com" rel="preconnect" />
    <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect" />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;900&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined"
      rel="stylesheet"
    />
    <script>
      tailwind.config = {
        darkMode: "class",
        theme: {
          extend: {
            colors: {
              primary: "#4396ea",
              "background-light": "#f6f7f8",
              "background-dark": "#111921",
            },
            fontFamily: {
              display: ["Inter", "sans-serif"],
            },
            borderRadius: {
              DEFAULT: "0.25rem",
              lg: "0.5rem",
              xl: "0.75rem",
              full: "9999px",
            },
          },
        },
      };
    </script>
    <link rel="stylesheet" href="/css/main.css" />
  </head>

  <body
    class="bg-background-light dark:bg-background-dark font-display text-[#0e141b] dark:text-slate-200 h-screen overflow-hidden"
  >
    <div class="flex h-full w-full">
      <%- include('../partials/siderbar_profesor', { active: active, user: user
      }) %>

      <main class="flex-1 p-8 h-full overflow-y-auto">
        <div class="max-w-4xl mx-auto">
          <header class="mb-8">
            <a
              href="/profesor/actividades"
              class="inline-flex items-center gap-1.5 rounded-lg px-3 py-1.5 text-sm font-medium mb-2 bg-primary/20 dark:bg-primary/30 text-primary dark:text-blue-300 hover:bg-primary/30 dark:hover:bg-primary/40 transition-colors"
            >
              <span class="material-symbols-outlined text-lg">arrow_back</span>
              Volver a la lista
            </a>
            <h1
              class="text-slate-900 dark:text-white text-4xl font-bold leading-tight"
            >
              Crear Nueva Actividad
            </h1>
            <p
              class="text-slate-500 dark:text-slate-400 text-base font-normal mt-2"
            >
              Completa los detalles de la lectura y añade las preguntas de
              comprensión.
            </p>
          </header>

          <form
            action="/profesor/actividades/crear"
            method="POST"
            enctype="multipart/form-data"
            class="space-y-8"
          >
            <div
              class="bg-white dark:bg-slate-900 rounded-xl shadow-sm border border-slate-200 dark:border-slate-700 p-6"
            >
              <div
                class="flex flex-col sm:flex-row sm:items-center justify-between gap-4 mb-4 border-b border-slate-200 dark:border-slate-700 pb-2"
              >
                <h3 class="text-lg font-bold text-slate-900 dark:text-white">
                  Detalles de la Lectura
                </h3>

                <button
                  type="button"
                  id="btn-generar-ia"
                  class="inline-flex items-center gap-2 px-4 py-2 text-sm font-medium text-indigo-600 bg-indigo-50 hover:bg-indigo-100 dark:text-indigo-300 dark:bg-indigo-900/30 dark:hover:bg-indigo-900/50 rounded-lg transition-colors border border-indigo-100 dark:border-indigo-800"
                >
                  <span class="material-symbols-outlined text-[20px]"
                    >auto_awesome</span
                  >
                  <span>Generar Contenido con IA</span>
                </button>
              </div>
              <div class="space-y-4 pt-2">
                <div>
                  <label
                    class="block text-sm font-medium text-slate-600 dark:text-slate-300 mb-1"
                    for="titulo"
                    >Título</label
                  >
                  <input
                    class="w-full bg-slate-100 dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg focus:ring-primary focus:border-primary"
                    id="titulo"
                    name="titulo"
                    type="text"
                    required
                  />
                </div>
                <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                  <div>
                    <label
                      class="block text-sm font-medium text-slate-600 dark:text-slate-300 mb-1"
                      for="tema"
                      >Tema</label
                    >
                    <input
                      class="w-full bg-slate-100 dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg focus:ring-primary focus:border-primary"
                      id="tema"
                      name="tema"
                      type="text"
                      required
                    />
                  </div>
                  <div>
                    <label
                      class="block text-sm font-medium text-slate-600 dark:text-slate-300 mb-1"
                      for="dificultad"
                      >Dificultad</label
                    >
                    <select
                      class="w-full bg-slate-100 dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg focus:ring-primary focus:border-primary"
                      id="dificultad"
                      name="dificultad"
                      required
                    >
                      <option value="Fácil">Fácil</option>
                      <option value="Intermedio">Intermedio</option>
                      <option value="Difícil">Difícil</option>
                    </select>
                  </div>
                </div>
                <div>
                  <label
                    class="block text-sm font-medium text-slate-600 dark:text-slate-300 mb-1"
                    for="imagen"
                    >Imagen de Portada</label
                  >
                  <input
                    class="w-full text-sm text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-lg file:border-0 file:text-sm file:font-semibold file:bg-primary/10 file:text-primary hover:file:bg-primary/20 dark:file:bg-primary/20 dark:file:text-blue-300"
                    id="imagen"
                    name="imagen"
                    type="file"
                    accept="image/*"
                  />
                </div>
                <div>
                  <label
                    class="block text-sm font-medium text-slate-600 dark:text-slate-300 mb-1"
                    for="descripcion"
                    >Texto de la Lectura</label
                  >
                  <textarea
                    class="w-full bg-slate-100 dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg focus:ring-primary focus:border-primary"
                    id="descripcion"
                    name="descripcion"
                    rows="10"
                    required
                  ></textarea>
                </div>
              </div>
            </div>

            <div
              class="bg-white dark:bg-slate-900 rounded-xl shadow-sm border border-slate-200 dark:border-slate-700 p-6"
            >
              <h3
                class="text-lg font-bold text-slate-900 dark:text-white mb-4 border-b border-slate-200 dark:border-slate-700 pb-2"
              >
                Preguntas de Comprensión
              </h3>

              <div id="preguntas-container" class="space-y-6 pt-2">
                <div
                  class="pregunta-bloque border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800/50 rounded-lg p-4 relative"
                  data-index="0"
                >
                  <button
                    type="button"
                    onclick="eliminarPregunta(this)"
                    class="absolute top-3 right-3 text-slate-400 hover:text-red-500 transition-colors"
                    title="Eliminar pregunta"
                  >
                    <span class="material-symbols-outlined">delete</span>
                  </button>

                  <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-4">
                    <div class="md:col-span-3">
                      <label
                        class="block text-sm font-medium text-slate-600 dark:text-slate-300 mb-1"
                        for="preguntas[0][preguntaTexto]"
                        >Texto de la Pregunta 1</label
                      >
                      <input
                        class="w-full bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg focus:ring-primary focus:border-primary"
                        name="preguntas[0][preguntaTexto]"
                        type="text"
                        required
                      />
                    </div>
                    <div class="md:col-span-1">
                      <label
                        class="block text-sm font-medium text-slate-600 dark:text-slate-300 mb-1"
                        for="preguntas[0][puntaje]"
                        >Puntaje</label
                      >
                      <input
                        class="w-full bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg focus:ring-primary focus:border-primary"
                        name="preguntas[0][puntaje]"
                        type="number"
                        value="10"
                        min="1"
                        required
                      />
                    </div>
                  </div>

                  <p
                    class="text-sm font-medium text-slate-700 dark:text-slate-300 mb-2"
                  >
                    Alternativas:
                  </p>
                  <div
                    class="alternativas-container space-y-2"
                    data-pregunta-index="0"
                  >
                    <div class="alternativa-item flex items-center gap-3">
                      <input
                        type="radio"
                        name="preguntas[0][correcta]"
                        value="0"
                        class="w-4 h-4 text-primary bg-slate-100 border-slate-300 focus:ring-primary dark:bg-slate-700 dark:border-slate-600"
                        required
                      />
                      <input
                        type="text"
                        name="preguntas[0][alternativas][0][texto]"
                        placeholder="Escribe una alternativa..."
                        class="flex-grow bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg text-sm focus:ring-primary focus:border-primary py-2 px-3"
                        required
                      />
                      <button
                        type="button"
                        onclick="eliminarAlternativa(this)"
                        class="text-slate-400 hover:text-red-500 transition-colors"
                      >
                        <span class="material-symbols-outlined text-lg"
                          >remove_circle_outline</span
                        >
                      </button>
                    </div>
                  </div>
                  <button
                    type="button"
                    onclick="agregarAlternativa(this)"
                    class="mt-3 inline-flex items-center gap-1 text-sm text-primary hover:text-blue-600 font-medium transition-colors"
                  >
                    <span class="material-symbols-outlined text-lg">add</span>
                    Añadir Alternativa
                  </button>
                </div>
              </div>

              <div
                class="mt-6 border-t border-slate-200 dark:border-slate-700 pt-4"
              >
                <button
                  type="button"
                  onclick="agregarPregunta()"
                  class="flex items-center justify-center w-full py-3 border-2 border-dashed border-slate-300 dark:border-slate-600 rounded-xl text-slate-600 dark:text-slate-400 hover:border-primary hover:text-primary hover:bg-primary/5 transition-all font-medium"
                >
                  <span class="material-symbols-outlined mr-2"
                    >add_circle_outline</span
                  >
                  Añadir Nueva Pregunta
                </button>
              </div>
            </div>

            <div class="flex justify-end gap-3 pt-4">
              <a
                href="/profesor/actividades"
                class="inline-flex min-w-[84px] items-center justify-center rounded-lg bg-slate-100 px-4 py-2.5 text-sm font-semibold text-slate-700 transition-colors hover:bg-slate-200 dark:bg-slate-800 dark:text-slate-300 dark:hover:bg-slate-700"
              >
                Cancelar
              </a>
              <button
                class="inline-flex min-w-[84px] items-center justify-center rounded-lg bg-primary px-6 py-2.5 text-sm font-semibold text-white transition-colors hover:bg-blue-600 focus:outline-none focus:ring-2 focus:ring-primary/50 focus:ring-offset-2 dark:focus:ring-offset-background-dark shadow-md"
                type="submit"
              >
                Publicar Actividad
              </button>
            </div>
          </form>
        </div>
      </main>
    </div>

    <script>
      let preguntaIndex = 1;

      function agregarPregunta() {
        const container = document.getElementById("preguntas-container");
        const nuevoIndex = preguntaIndex++;

        // NOTA: Aquí usamos las mismas clases CSS que en el HTML de arriba
        const template = `
        <div class="pregunta-bloque border border-slate-200 dark:border-slate-700 bg-slate-50 dark:bg-slate-800/50 rounded-lg p-4 relative mt-4" data-index="${nuevoIndex}">
            <button type="button" onclick="eliminarPregunta(this)" class="absolute top-3 right-3 text-slate-400 hover:text-red-500 transition-colors">
                <span class="material-symbols-outlined">delete</span>
            </button>

            <div class="grid grid-cols-1 md:grid-cols-4 gap-4 mb-4">
                <div class="md:col-span-3">
                    <label class="block text-sm font-medium text-slate-600 dark:text-slate-300 mb-1" for="preguntas[${nuevoIndex}][preguntaTexto]">Texto de la Pregunta ${nuevoIndex + 1}</label>
                    <input class="w-full bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg focus:ring-primary focus:border-primary" name="preguntas[${nuevoIndex}][preguntaTexto]" type="text" required/>
                </div>
                <div class="md:col-span-1">
                    <label class="block text-sm font-medium text-slate-600 dark:text-slate-300 mb-1" for="preguntas[${nuevoIndex}][puntaje]">Puntaje</label>
                    <input class="w-full bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg focus:ring-primary focus:border-primary" name="preguntas[${nuevoIndex}][puntaje]" type="number" value="10" min="1" required/>
                </div>
            </div>

            <p class="text-sm font-medium text-slate-700 dark:text-slate-300 mb-2">Alternativas:</p>
            <div class="alternativas-container space-y-2" data-pregunta-index="${nuevoIndex}">
                <div class="alternativa-item flex items-center gap-3">
                    <input type="radio" name="preguntas[${nuevoIndex}][correcta]" value="0" class="w-4 h-4 text-primary bg-slate-100 border-slate-300 focus:ring-primary dark:bg-slate-700 dark:border-slate-600" required>
                    <input type="text" name="preguntas[${nuevoIndex}][alternativas][0][texto]" placeholder="Escribe una alternativa..." class="flex-grow bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg text-sm focus:ring-primary focus:border-primary py-2 px-3">
                    <button type="button" onclick="eliminarAlternativa(this)" class="text-slate-400 hover:text-red-500 transition-colors">
                        <span class="material-symbols-outlined text-lg">remove_circle_outline</span>
                    </button>
                </div>
            </div>
            <button type="button" onclick="agregarAlternativa(this)" class="mt-3 inline-flex items-center gap-1 text-sm text-primary hover:text-blue-600 font-medium transition-colors">
                <span class="material-symbols-outlined text-lg">add</span>
                Añadir Alternativa
            </button>
        </div>`;

        container.insertAdjacentHTML("beforeend", template);
        actualizarIndices();
      }

      function eliminarPregunta(button) {
        const bloque = button.closest(".pregunta-bloque");
        if (bloque) {
          bloque.remove();
          actualizarIndices();
        }
      }

      function agregarAlternativa(button) {
        const container = button.previousElementSibling;
        const preguntaIndex = container.dataset.preguntaIndex;
        const alternativaIndex = container.children.length;

        const template = `
        <div class="alternativa-item flex items-center gap-3">
            <input type="radio" name="preguntas[${preguntaIndex}][correcta]" value="${alternativaIndex}" class="w-4 h-4 text-primary bg-slate-100 border-slate-300 focus:ring-primary dark:bg-slate-700 dark:border-slate-600">
            <input type="text" name="preguntas[${preguntaIndex}][alternativas][${alternativaIndex}][texto]" placeholder="Escribe una alternativa..." class="flex-grow bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg text-sm focus:ring-primary focus:border-primary py-2 px-3">
            <button type="button" onclick="eliminarAlternativa(this)" class="text-slate-400 hover:text-red-500 transition-colors">
                <span class="material-symbols-outlined text-lg">remove_circle_outline</span>
            </button>
        </div>`;
        container.insertAdjacentHTML("beforeend", template);
      }

      function eliminarAlternativa(button) {
        const item = button.closest(".alternativa-item");
        const container = item.parentElement;
        if (container.children.length > 1) {
          item.remove();
          // Reajustar indices (radio y text inputs)
          const radios = container.querySelectorAll('input[type="radio"]');
          radios.forEach((radio, index) => {
            radio.value = index;
          });

          const inputs = container.querySelectorAll('input[type="text"]');
          const preguntaIndex = container.dataset.preguntaIndex;
          inputs.forEach((input, index) => {
            input.name = `preguntas[${preguntaIndex}][alternativas][${index}][texto]`;
          });
        } else {
          alert("Cada pregunta debe tener al menos una alternativa.");
        }
      }

      function actualizarIndices() {
        const bloques = document.querySelectorAll(".pregunta-bloque");
        preguntaIndex = bloques.length;
        bloques.forEach((bloque, pIndex) => {
          bloque.dataset.index = pIndex;
          bloque.querySelector(`label[for^='preguntas[']`).textContent =
            `Texto de la Pregunta ${pIndex + 1}`;
          bloque.querySelector(
            `input[name^='preguntas['][name$='[preguntaTexto]']`,
          ).name = `preguntas[${pIndex}][preguntaTexto]`;
          bloque.querySelector(
            `input[name^='preguntas['][name$='[puntaje]']`,
          ).name = `preguntas[${pIndex}][puntaje]`;

          const altContainer = bloque.querySelector(".alternativas-container");
          altContainer.dataset.preguntaIndex = pIndex;
          const alternativasItems =
            altContainer.querySelectorAll(".alternativa-item");
          alternativasItems.forEach((altItem, aIndex) => {
            altItem.querySelector('input[type="radio"]').name =
              `preguntas[${pIndex}][correcta]`;
            altItem.querySelector('input[type="radio"]').value = aIndex;
            altItem.querySelector('input[type="text"]').name =
              `preguntas[${pIndex}][alternativas][${aIndex}][texto]`;
          });
        });
      }
      // --- FUNCIONES DE IA ---

      // 1. Abrir/Cerrar Modal
      function toggleModalIA(show) {
        const modal = document.getElementById("modal-ia");
        if (show) {
          modal.classList.remove("hidden");
        } else {
          modal.classList.add("hidden");
        }
      }

      // Conectar el botón que pusimos arriba con el modal
      const btnIA = document.getElementById("btn-generar-ia");
      if (btnIA) {
        btnIA.addEventListener("click", () => toggleModalIA(true));
      }

      // 2. Lógica Principal: Llamar al Backend y Rellenar
      async function ejecutarGeneracionIA() {
        const loading = document.getElementById("ia-loading");

        // Capturamos los valores
        const tema = document.getElementById("ia-tema").value;
        const grado = document.getElementById("ia-grado").value;
        const dificultad = document.getElementById("ia-dificultad").value;
        const cantPreguntas = document.getElementById("ia-preguntas").value;
        const cantAlternativas =
          document.getElementById("ia-alternativas").value;
        const puntajeTotal = document.getElementById("ia-puntaje").value; // <--- CAPTURAR

        if (!tema || !grado) {
          alert("Por favor completa el tema y el grado.");
          return;
        }

        loading.classList.remove("hidden");

        try {
          const response = await fetch("/profesor/actividades/generar-ia", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify({
              tema,
              grado,
              dificultad,
              cantidad_preguntas: cantPreguntas,
              cantidad_alternativas: cantAlternativas,
              puntaje_total: puntajeTotal, // <--- ENVIAR
            }),
          });

          const result = await response.json();

          if (result.success) {
            rellenarFormulario(result.data);
            toggleModalIA(false);
          } else {
            alert("Error: " + result.message);
          }
        } catch (error) {
          console.error(error);
          alert("Hubo un error al conectar con el servidor.");
        } finally {
          loading.classList.add("hidden");
        }
      }

      // 3. La Magia: Llenar los inputs automáticamente
      function rellenarFormulario(data) {
        // Llenar datos básicos
        document.getElementById("titulo").value = data.titulo;
        document.getElementById("tema").value = data.tema;
        document.getElementById("dificultad").value = data.dificultad;
        document.getElementById("descripcion").value = data.descripcion;

        // Limpiar preguntas existentes (opcional, o dejar la que está por defecto)
        const contenedor = document.getElementById("preguntas-container");
        contenedor.innerHTML = "";
        preguntaIndex = 0; // Reiniciar contador global

        // Iterar sobre las preguntas generadas
        data.preguntas.forEach((preg, index) => {
          // 1. Crear la estructura visual de la pregunta
          agregarPregunta();

          // Como agregarPregunta() es síncrono, el nuevo bloque es el último hijo
          const bloques = document.querySelectorAll(".pregunta-bloque");
          const ultimoBloque = bloques[bloques.length - 1];
          const idx = ultimoBloque.dataset.index; // El índice real generado

          // 2. Llenar Texto y Puntaje
          ultimoBloque.querySelector(
            `input[name^="preguntas["][name$="[preguntaTexto]"]`,
          ).value = preg.preguntaTexto;
          ultimoBloque.querySelector(
            `input[name^="preguntas["][name$="[puntaje]"]`,
          ).value = preg.puntaje;

          // 3. Gestionar Alternativas
          const altContainer = ultimoBloque.querySelector(
            ".alternativas-container",
          );
          altContainer.innerHTML = ""; // Limpiar la alternativa por defecto

          preg.alternativas.forEach((alt, altIndex) => {
            // Usamos tu función existente (simulamos el botón)
            // Pero necesitamos un "botón ficticio" para pasarle a tu función agregarAlternativa
            // O mejor, inyectamos el HTML directo usando tu lógica de template para ser más rápidos

            const template = `
            <div class="alternativa-item flex items-center gap-3">
                <input type="radio" name="preguntas[${idx}][correcta]" value="${altIndex}" class="w-4 h-4 text-primary bg-slate-100 border-slate-300 focus:ring-primary dark:bg-slate-700 dark:border-slate-600" ${alt.esCorrecta ? "checked" : ""}>
                <input type="text" name="preguntas[${idx}][alternativas][${altIndex}][texto]" value="${alt.texto}" class="flex-grow bg-white dark:bg-slate-800 border-slate-200 dark:border-slate-700 rounded-lg text-sm focus:ring-primary focus:border-primary py-2 px-3">
                <button type="button" onclick="eliminarAlternativa(this)" class="text-slate-400 hover:text-red-500 transition-colors">
                   <span class="material-symbols-outlined text-lg">remove_circle_outline</span>
                </button>
            </div>`;
            altContainer.insertAdjacentHTML("beforeend", template);
          });
        });
      }
    </script>
    <div
      id="modal-ia"
      class="fixed inset-0 z-50 hidden"
      aria-labelledby="modal-title"
      role="dialog"
      aria-modal="true"
    >
      <div
        class="fixed inset-0 bg-slate-900/50 backdrop-blur-sm transition-opacity"
      ></div>

      <div class="fixed inset-0 z-10 w-screen overflow-y-auto">
        <div
          class="flex min-h-full items-end justify-center p-4 text-center sm:items-center sm:p-0"
        >
          <div
            class="relative transform overflow-hidden rounded-xl bg-white dark:bg-slate-900 text-left shadow-xl transition-all sm:my-8 sm:w-full sm:max-w-lg border border-slate-200 dark:border-slate-700"
          >
            <div
              class="bg-indigo-50 dark:bg-indigo-900/20 px-4 py-4 sm:px-6 border-b border-indigo-100 dark:border-indigo-800"
            >
              <h3
                class="text-lg font-bold leading-6 text-indigo-900 dark:text-indigo-200 flex items-center gap-2"
                id="modal-title"
              >
                <span class="material-symbols-outlined">auto_awesome</span>
                Generador de Actividades IA
              </h3>
            </div>

            <div class="px-4 py-5 sm:p-6 space-y-4">
              <div>
                <label
                  class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1"
                  >Tema de la Lectura</label
                >
                <input
                  type="text"
                  id="ia-tema"
                  class="w-full rounded-lg border-slate-300 dark:border-slate-600 bg-slate-50 dark:bg-slate-800 text-sm"
                  placeholder="Ej: La Revolución Industrial"
                />
              </div>

              <div class="grid grid-cols-2 gap-4">
                <div>
                  <label
                    class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1"
                    >Grado</label
                  >
                  <input
                    type="text"
                    id="ia-grado"
                    class="w-full rounded-lg border-slate-300 dark:border-slate-600 bg-slate-50 dark:bg-slate-800 text-sm"
                    placeholder="Ej: 5to Secundaria"
                  />
                </div>
                <div>
                  <label
                    class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1"
                    >Dificultad</label
                  >
                  <select
                    id="ia-dificultad"
                    class="w-full rounded-lg border-slate-300 dark:border-slate-600 bg-slate-50 dark:bg-slate-800 text-sm"
                  >
                    <option value="Fácil">Fácil</option>
                    <option value="Intermedio" selected>Intermedio</option>
                    <option value="Difícil">Difícil</option>
                  </select>
                </div>
              </div>

              <div class="grid grid-cols-3 gap-4">
                <div>
                  <label
                    class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1"
                    >N° Preguntas</label
                  >
                  <input
                    type="number"
                    id="ia-preguntas"
                    value="4"
                    min="1"
                    max="10"
                    class="w-full rounded-lg border-slate-300 dark:border-slate-600 bg-slate-50 dark:bg-slate-800 text-sm"
                  />
                </div>
                <div>
                  <label
                    class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1"
                    >Alternativas</label
                  >
                  <input
                    type="number"
                    id="ia-alternativas"
                    value="4"
                    min="2"
                    max="5"
                    class="w-full rounded-lg border-slate-300 dark:border-slate-600 bg-slate-50 dark:bg-slate-800 text-sm"
                  />
                </div>
                <div>
                  <label
                    class="block text-sm font-medium text-slate-700 dark:text-slate-300 mb-1"
                    >Puntaje Total</label
                  >
                  <input
                    type="number"
                    id="ia-puntaje"
                    value="20"
                    min="1"
                    max="100"
                    class="w-full rounded-lg border-slate-300 dark:border-slate-600 bg-slate-50 dark:bg-slate-800 text-sm font-bold text-indigo-600"
                  />
                </div>
              </div>

              <div id="ia-loading" class="hidden text-center py-4">
                <div
                  class="inline-block animate-spin rounded-full h-8 w-8 border-4 border-indigo-500 border-t-transparent"
                ></div>
                <p class="text-sm text-indigo-600 mt-2 font-medium">
                  Generando contenido mágico...
                </p>
              </div>
            </div>

            <div
              class="bg-slate-50 dark:bg-slate-800/50 px-4 py-3 sm:flex sm:flex-row-reverse sm:px-6 gap-2"
            >
              <button
                type="button"
                onclick="ejecutarGeneracionIA()"
                class="inline-flex w-full justify-center rounded-lg bg-indigo-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 sm:w-auto"
              >
                ✨ Generar
              </button>
              <button
                type="button"
                onclick="toggleModalIA(false)"
                class="mt-3 inline-flex w-full justify-center rounded-lg bg-white dark:bg-slate-800 px-3 py-2 text-sm font-semibold text-slate-900 dark:text-slate-300 shadow-sm ring-1 ring-inset ring-slate-300 dark:ring-slate-600 hover:bg-slate-50 sm:mt-0 sm:w-auto"
              >
                Cancelar
              </button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
